<div class="tooltip">
  {% if proyecto.mainImage%}
  <picture>
    {% for format, images in proyecto.mainImage %}
    <source
      srcset="{{ images | srcset }}"
      sizes="(max-width: 1200px) 100vw, 1200px"
      type="image/{{ format }}"
    />
    {% endfor %}
    <img
      src="{{ proyecto.mainImage.jpeg[0].url }}"
      alt="{{ proyecto.mainImage.Alt }}"
    />
  </picture>
  {% endif %}
</div>
